import React from 'react'

const GetData = () => {
  return (
    <div style={{ padding:'10px' }}>
      <span className="annualized-recovery-value2" style={{fontSize:20}}>
        Annualized Recovery Value
      </span>  
      <span className="per-rule" style={{fontSize:18}}>{` per Rule `}</span>
    </div>
  )
}

export default GetData
